# Windows manual QA

- `1-click-kb-switch-setup.exe` installs successfully
- Installer offers to launch 1-Click-KB-Switch at the end
- First launch opens main window
- Next launch starts hidden in tray
- Tray menu contains only Show main window and Exit
- Single-instance guard blocks a second launch
- HTML session log is recreated on startup in the user log directory
- LeftCtrl switches to English layout
- LeftShift switches to first non-English layout
- Tray label uses normal Latin glyphs
